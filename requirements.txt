# SSL-GraphAnomaly — pinned dependency list
# Targets Python >= 3.10, PyTorch >= 2.3, CUDA 12.1

# --- Deep learning ----------------------------------------------------------
torch>=2.3.0
torchvision>=0.18.0
torchaudio>=2.3.0
torch-geometric>=2.5.0
torch-scatter>=2.1.2
torch-sparse>=0.6.18

# --- Scientific stack -------------------------------------------------------
numpy>=1.26.0
scipy>=1.13.0
scikit-learn>=1.4.0
pandas>=2.2.0
pyarrow>=15.0.0

# --- Conformal prediction utilities ----------------------------------------
mapie>=0.8.4

# --- Networking / dataset acquisition --------------------------------------
kaggle>=1.6.0
requests>=2.32.0
tqdm>=4.66.0

# --- Visualisation ----------------------------------------------------------
matplotlib>=3.8.0
seaborn>=0.13.0

# --- Serving ----------------------------------------------------------------
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0

# --- Configuration ----------------------------------------------------------
pyyaml>=6.0.1
omegaconf>=2.3.0
hydra-core>=1.3.2
python-dotenv>=1.0.0
click>=8.1.7
rich>=13.7.0

# --- Adversarial robustness ------------------------------------------------
torchattacks>=3.5.1

# --- Baselines (Kitsune autoencoder ensemble dependency) ------------------
networkx>=3.3

# --- Testing / linting ------------------------------------------------------
pytest>=8.2.0
pytest-cov>=5.0.0
black>=24.4.0
ruff>=0.4.0
mypy>=1.10.0
